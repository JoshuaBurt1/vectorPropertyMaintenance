{
  "name": "vector-property-maintenance",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy:web": "pnpm --filter web build && firebase deploy --only hosting:main-site",
    "mobile:android": "pnpm --filter mobile exec expo run:android --variant release",
    "m_w:android": "pnpm --filter m_w exec expo run:android --variant release"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "sharp",
      "unrs-resolver"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "turbo": "^2.9.6",
    "typescript": "^6.0.3"
  }
}